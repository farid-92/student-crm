<div class="page-header">
  <div class="ui breadcrumb">
    <%= link_to 'Все курсы', root_path %>
    <div class="divider"> / </div>
    <%= link_to  @group.course.name, show_course_index_path(@group.course, resource: 2) %>
    <div class="divider"> / </div>
    <%= link_to  @group.name, show_group_index_path(@group, resource: 2) %>
    <div class="divider"> / </div>
    <div class="active section"><%= @period.title %></div>
    <div class="divider"> / </div>
  </div>
  <h5 class="text-center">Раздатки для занятия <%= @period.title %></h5>
</div>
<%= render 'shared/course_themes' %>