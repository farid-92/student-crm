<% provide(:btn, 'Редактировать занятие') %>
<div class="page-header">
  <div class="ui breadcrumb">
    <%= link_to 'Все курсы', courses_path(@group.course) %>
    <div class="divider"> / </div>
    <%= link_to  @group.course.name, show_course_index_path(@group.course, resource: 2) %>
    <div class="divider"> / </div>
    <%= link_to  @group.name, show_group_index_path(@group, resource: 2) %>
    <div class="divider"> / </div>
    <div class="active section">Редактирование занятия</div>
  </div>
  <h5 class="text-center">Редактирование занятия</h5>
</div>
<%= render 'periods/period_form' %>